# Optional MCP servers

| MCP | Why |
|---|---|
| Playwright | Visual QA of local `http://localhost:<WP_PORT>` |
| Context7 | Current WordPress API docs |
| Hostinger | Production DB/files (`npx -y @hostinger/mcp --login`) |

See skill **hostinger-mcp-deploy** for Hostinger wiring. Tokens never belong in git.
