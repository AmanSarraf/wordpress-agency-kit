# Unique per client. Prefixes Docker volumes/containers.
COMPOSE_PROJECT_NAME=client-slug

# Change when running several clients at once. PMA_PORT = WP_PORT + 1
WP_PORT=8080
PMA_PORT=8081

DB_NAME=wordpress
DB_USER=wpuser
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-root

WP_TABLE_PREFIX=wp_
